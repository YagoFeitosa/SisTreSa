<h1>Médicos</h1>

<ul>
  <% @medicos.each do |medico| %>
    <li>
        <a href="<%= medico_path(medico) %>">
            <%= medico.nome %>
        </a>
    </li>
  <% end %>
  <%= link_to "Novo Medico", new_medico_path %>
</ul>